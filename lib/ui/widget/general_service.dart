import 'package:flutter/material.dart';

class GeneralServiceWidget extends StatelessWidget {
  final bool isSelected;

  GeneralServiceWidget(this.isSelected);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
